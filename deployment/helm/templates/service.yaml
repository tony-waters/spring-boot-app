apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-jpa.fullname" . }}
  labels:
    {{- include "spring-jpa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "spring-jpa.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP